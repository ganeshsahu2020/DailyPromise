"use client";

/**
 * DEPRECATED: Your app uses createBrowserRouter in src/main.tsx
 * as the single router. Keeping this file as a NO-OP avoids
 * accidentally mounting a second <Routes> tree (which caused
 * errors and conflicts).
 */
export default function AppRoutes() {
  return null;
}
